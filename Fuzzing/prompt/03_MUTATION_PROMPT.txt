Given this BASE script (verbatim below), produce ONE mutated script that keeps it runnable and deterministic, while stressing different failure modes.

Mutation strategy: {MUTATION_STRATEGY}    # one of ["shape-scale","dtype-cast","nan-inject","rng-edge","layout-perturb","async-overlap"]
Constraints:
- Preserve allowed API set: {API_SET}
- Preserve at least {PRESERVE_RATIO}% of original chain nodes.
- Maintain dependency types: {DEPENDENCY_TYPES}.
- Keep runtime <= {RUNTIME_LIMIT_S} sec; reuse seed={SEED}.
- Keep CPU/GPU oracle and summary prints.
BASE SCRIPT:
{BASE_SCRIPT}
Return ONE complete mutated script only.
